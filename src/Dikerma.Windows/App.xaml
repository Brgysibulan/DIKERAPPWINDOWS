<Application x:Class="Dikerma.Windows.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="BrandGreen" Color="#146B3A" />
        <SolidColorBrush x:Key="BrandGreenDark" Color="#0B4D2A" />
        <SolidColorBrush x:Key="Surface" Color="#F5F7F6" />
        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="7" />
            <Setter Property="Margin" Value="0,3,0,7" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="6" />
            <Setter Property="Margin" Value="0,3,0,7" />
        </Style>
    </Application.Resources>
</Application>
